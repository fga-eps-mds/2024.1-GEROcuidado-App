import React from 'react';
import RedefinirSenha from '../components/RedefinirSenha'

export default function PasswordRecovery({ route, navigation })  {
    return (<RedefinirSenha navigation={navigation} />)

}